name: Test Repo

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: into-docker
        # You may pin to the exact commit or the version.
        # uses: into-docker/build-action@1238c17c8196f8ba04329437262dd4040afed85e
        uses: into-docker/build-action@v1
        with:
          image: ./Dockerfile
